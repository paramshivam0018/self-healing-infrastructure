apiVersion: v1
kind: Service
metadata:
  name: self-healing-service
  namespace: default
  labels:
    app: self-healing-app
spec:
  selector:
    app: self-healing-app
  type: NodePort
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8000
    nodePort: 30080
